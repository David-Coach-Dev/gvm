name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build Windows binaries
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: windows
            goarch: amd64
            suffix: .exe
          - goos: windows
            goarch: 386
            suffix: .exe

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    - name: Build binary
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
        CGO_ENABLED: 0
      run: |
        VERSION=${GITHUB_REF#refs/tags/}
        go build -ldflags="-s -w -X 'main.version=$VERSION'" -o gvm-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.suffix }} main.go

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: gvm-${{ matrix.goos }}-${{ matrix.goarch }}
        path: gvm-*

  release:
    name: Create release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
    - uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Prepare release files
      run: |
        mkdir -p release
        find . -name "gvm-*" -type f -exec cp {} release/ \;

        # Create main executables (like NVM-Windows style)
        if [ -f "release/gvm-windows-amd64.exe" ]; then
          cp release/gvm-windows-amd64.exe release/gvm.exe
          cp release/gvm-windows-amd64.exe release/gvm-setup.exe
        fi

        # Create Windows-specific checksums
        cd release
        sha256sum * > checksums.txt
        md5sum * > md5sums.txt

        # Show Windows release files
        ls -la

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: release/*
        draft: false
        prerelease: false
        generate_release_notes: true
        body: |
          ## 🚀 Installation

          ### One-line Installation (recommended)

          **PowerShell (Windows):**
          ```powershell
          iwr -useb https://raw.githubusercontent.com/David-Coach-Dev/gvm/main/install-nvm-style.ps1 | iex
          ```

          **Command Prompt (Windows):**
          ```cmd
          curl -o install-gvm.bat https://raw.githubusercontent.com/David-Coach-Dev/gvm/main/install-gvm.bat && install-gvm.bat
          ```

          ### Manual Installation

          1. Download `gvm-setup.exe` or `gvm.exe`
          2. Run the installer or add binary to your PATH
          3. Start using GVM!

          ## 📦 Assets

          - **`gvm-setup.exe`** - Main Windows installer (64-bit)
          - **`gvm.exe`** - Windows binary (64-bit)
          - **`gvm-windows-amd64.exe`** - Windows binary (64-bit)
          - **`gvm-windows-386.exe`** - Windows binary (32-bit)
          - **`checksums.txt`** - SHA256 checksums
          - **`md5sums.txt`** - MD5 checksums

          ## 🎯 Commands

          ```cmd
          gvm install 1.21.0    # Install Go 1.21.0
          gvm use 1.21.0        # Switch to Go 1.21.0
          gvm list              # List installed versions
          gvm listall           # List available versions
          gvm uninstall 1.20.5  # Remove Go 1.20.5
          ```

          ## 💡 Features

          - ✅ **Windows Only** - Optimized specifically for Windows
          - ✅ **Zero Dependencies** - No Go required for installation
          - ✅ **NVM-inspired** - Familiar commands and workflow
          - ✅ **Auto-switching** - Support for `.go-version` files
          - ✅ **Shell Integration** - PowerShell and Command Prompt
          - ✅ **32-bit & 64-bit** - Support for both architectures

          > **Note:** For Linux/macOS, use the original [GVM](https://github.com/moovweb/gvm)

          ---

          **Full Changelog:** [${{ github.event.before }}...${{ github.sha }}](https://github.com/${{ github.repository }}/compare/${{ github.event.before }}...${{ github.sha }})
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
